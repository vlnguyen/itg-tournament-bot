import { Text, Title } from '@mantine/core';

/** Placeholder for the organizer console — the run view lands in a later step. */
export default function ConsoleHome(): JSX.Element {
  return (
    <>
      <Title order={1}>Organizer Console</Title>
      <Text>Sign in required — coming in a later build step.</Text>
    </>
  );
}
