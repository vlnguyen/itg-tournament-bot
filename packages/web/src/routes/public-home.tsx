import { Text, Title } from '@mantine/core';

/** Placeholder for the public surface — landing redirect to the active tournament's bracket lands in a later step. */
export default function PublicHome(): JSX.Element {
  return (
    <>
      <Title order={1}>ITG Tournament Bot</Title>
      <Text>Public bracket and results — coming in a later build step.</Text>
    </>
  );
}
